import React from 'react';
import './Header.css';
import FixedBackground from './FixedBackground';

function Header() {
  return (
    <header id="home">
      <FixedBackground />
    </header>
  );
}

export default Header;
